spring:
  application:
    name: spring_boot_project
  output:
    ansi:
      enabled: ALWAYS
  devtools:
    restart:
      enabled: true

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/spring_boot_naum_datebase?useTimezone=true&serverTimezone=UTC
    username: root
    password: admin123

  jpa:
    hibernate:
      ddl-auto: none
#    properties:
#      dialect: org.hibernate.dialect.MySQLDialect
    show-sql: false
    open-in-view: false